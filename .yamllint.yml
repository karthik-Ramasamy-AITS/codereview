extends: default

rules:
  indentation:
    spaces: 2
    indent-sequences: true

  line-length:
    max: 120

  trailing-spaces: enable

  empty-lines:
    max: 1

  key-duplicates: enable

  hyphens:
    max-spaces-after: 1

  comments:
    require-starting-space: true
